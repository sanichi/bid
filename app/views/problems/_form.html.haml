:ruby
  inputs = [
    [:hand,     t("problem.hand"),     :text, maxlength: Problem::MAX_HAND],
    [:bids,     t("problem.bids"),     :text],
    [:vul,      t("problem.vul"),      :select, menu: problem_vul_menu(problem.vul)],
    [:category, t("problem.category"), :text, maxlength: Problem::MAX_CATEGORY],
    [:note,     t("problem.note"),     :area, rows: 20],
  ]
= render "utils/crud/form", object: problem, index: problems_path, inputs: inputs, title: title, center: center(xl: 8, lg: 10)
