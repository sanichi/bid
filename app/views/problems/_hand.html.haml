- hand = Hand.new(problem.hand).hand

%table.hand-tab
  %tbody
    - hand.each_pair do |suit, cards|
      %tr
        %td.pe-1= show_suit(suit)
        %td
          - cards.each do |card|
            = card
