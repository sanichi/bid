- problems.matches.each do |problem|
  %tr
    %td= link_to problem.id, problem
    %td= problem.hand
    %td= problem.bids
    %td= t("problem.vuls.#{problem.vul}")
    %td.text-center= t("symbol.tick") if problem.draft
    %td= problem.user.name
= render "utils/pagination/links", pager: problems, columns: 6
